// Use DBML to define your database structure

Table users {
  id uuid [primary key]
  email text [not null, unique]

  password_hash text [not null]

  full_name text [not null]

  role text [not null]
  
  is_active boolean [not null, default: true]

  created_at timestamptz [not null, default: `now()`]

  updated_at timestamptz [not null, default: `now()`]

}

Table segments {
  id uuid [primary key]
  name text [not null, unique]
  description text
  created_at timestamptz [not null, default: `now()`]
}

Ref: customers.owner_user_id > users.id
Ref: customers.segment_id > segments.id
Table customers {
  id uuid [primary key]
  name text [not null]
  stage text [not null]
  owner_user_id uuid [not null]
  segment_id uuid
  attributes_json jsonb [not null]
  tags text[] [not null]
  last_interaction_at timestamptz
  next_action_due_at timestamptz
  created_at timestamptz [not null, default: `now()`]
  updated_at timestamptz [not null, default: `now()`]
}

Ref: contacts.customer_id > customers.id
Table contacts {
  id uuid [primary key]
  customer_id uuid [not null]
  type text [not null]
  value text [not null]
  is_primary boolean [not null, default: false]
  created_at timestamptz [not null, default: `now()`]
}


Ref: interactions.customer_id > customers.id
Ref: interactions.user_id > users.id
Table interactions {
  id uuid [primary key]
  user_id uuid [not null]
  customer_id uuid [not null]
  kind text [not null]
  occurred_at timestamptz [not null, default: `now()`]
  summary text [not null]
  outcome text
  channel text
  created_at timestamptz [not null, default: `now()`]
  }

Ref: tasks.customer_id > customers.id
Ref: tasks.assigned_user_id > users.id
Table tasks{
  id uuid [primary key]
  assigned_user_id uuid [not null]
  customer_id uuid [not null]
  type text [not null]
  status text [not null]
  title text [not null]
  description text
  due_at timestamptz [not null]
  priority integer [not null, default: 3]
  created_at timestamptz [not null, default: `now()`]
  updated_at timestamptz [not null, default: `now()`]
  closed_at timestamptz
}

Ref: templates.segment_id > segments.id
Table templates {
  id uuid [primary key]
  name text [not null]
  channel text [not null]
  content text [not null]
  segment_id uuid
  stage text
  is_active boolean [not null, default: true]
  created_at timestamptz [not null, default: `now()`]
  updated_at timestamptz [not null, default: `now()`]
}

Table rules {
  id uuid [primary key]
  name text [not null]
  is_active boolean [not null, default: true]
  priority integer [not null, default: 100]
  condition_json jsonb [not null]
  action_json jsonb [not null]
  created_at timestamptz [not null, default: `now()`]
  updated_at timestamptz [not null, default: `now()`]
}

Ref: quality_scores.customer_id > customers.id
Table quality_scores {
  id uuid [primary key]
  customer_id uuid [not null]
  period_start date [not null]
  period_end date [not null]
  first_response_seconds integer [not null, default: 0]
  overdue_tasks_count integer [not null, default: 0]
  interactions_count integer [not null, default: 0]
  nps_score integer
  created_at timestamptz [not null, default: `now()`]
}
